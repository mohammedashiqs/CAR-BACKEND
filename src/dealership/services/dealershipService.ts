
import { ObjectId } from 'mongodb';
import { IDealership } from '../models/IDealership';
import collections from '../../config/collections';
/* import { db } from '../../config/db'; */




/* const updateDealership = async (dealershipId: OjectId, dealershipDetails: IDealership) => {
        try{
            let updatedDealership = await db.collection(collections.DEALERSHIP_COLLECTION).findByIdAndUpdate(dealershipId, dealershipDetails, {new: true})
        }
} */